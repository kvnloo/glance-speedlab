# E000 — Instrumented live baseline

See the preregistration in `research/EXPERIMENTS.md`. This directory will hold the curated environment and aggregate result after a local model run. Raw traces stay in `research/runs/`.
